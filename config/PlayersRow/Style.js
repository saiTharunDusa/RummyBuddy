import { StyleSheet } from "react-native";

const Style = StyleSheet.create({
    headerRow: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 8,
      },
      roundLabel: {
        width: 30,
        fontWeight: 'bold',
        textAlign: 'center',
      },
})

export default Style;